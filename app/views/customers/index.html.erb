<% if cookies[:saved_subscriber] %>
    Thanks for Contacting Us!
<% else %>
    <h3 class="font-alt">Need Help With A Project?</h3>
        <h4 class="sub-header"> We’re available for your projects that must launch yesterday or tomorrow! 
        Share a preview of your requirements using the form below and a Margevity expert shall contact you shortly.</h4>
    <%= form_for @customer, :html => { :class =>'sign-up-form' } do |f| %>
        <%= f.text_field :first_name, placeholder: "First Name", class: 'label' %>
        <%= f.text_field :last_name, placeholder: "Last Name", class: 'label' %>
        <%= f.email_field :email, placeholder: "Email", class: 'label' %>
        <%= f.text_field :your_business, placeholder: "Tell us about your business", class: 'label large-box' %>
        <%= f.text_field :looking_for, placeholder: "What services are you looking for?", class: 'label large-box' %>
        <%= f.submit "Submit", type: "button", class: "simple", id: "simple" %>
    <% end %>
<% end  %>
<%= link_to "Go Back", root_path, class: "btn btn-ghost", id: 'go-back' %>